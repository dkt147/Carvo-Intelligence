# Port the intelligence service listens on (backend expects AI_SERVICE_URL=http://localhost:8000)
PORT=8000

# Shared secret with the backend. If empty, the service accepts unauthenticated requests
# (the backend only sends "Authorization: Bearer <key>" when AI_SERVICE_API_KEY is set).
AI_SERVICE_API_KEY=

# ---- LLM: groq (default) or openai. The process will not start without a key. ----
# Put the key for the chosen LLM_PROVIDER here. There is no Groq/OpenAI fallback.
# OpenAI-compatible hosts (e.g. Ollama): LLM_PROVIDER=openai plus LLM_BASE_URL / LLM_MODEL.
LLM_PROVIDER=groq
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=

# ---- Embeddings for protocol retrieval ----
# "local" = free offline model via fastembed (no API key, ~130 MB one-time download).
# "openai" = text-embedding-3-small (needs OPENAI_API_KEY below; not used for the LLM).
EMBEDDINGS_PROVIDER=local
LOCAL_EMBEDDING_MODEL=intfloat/multilingual-e5-small
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_KEY=

# ---- Retrieval ----
PROTOCOLS_DIR=D:/protocolscarvo
INDEX_DIR=data/index
RETRIEVAL_TOP_K=6

LOG_LEVEL=INFO
